import math
def find_lcm(x, y):
    return abs(x * y) // math.gcd(x, y)
 
def find_gcd(x, y):
    return math.gcd(x, y)
 
# Example usage
x = 12
y = 18
print("LCM:", find_lcm(x, y))
print("GCD:", find_gcd(x, y))
